# MLflow
MLFLOW_TRACKING_URI=mlruns
MLFLOW_EXPERIMENT_NAME=bank_marketing_classification

# Model registry path (where packaged models will be saved/found)
MODEL_DIR=models

# API
API_MODEL_PATH=models/latest

# DVC remote example (optional)
# DVC_REMOTE=s3://your-bucket/path
